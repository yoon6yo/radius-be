apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: radius
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  # ClusterIP로 한정: 클러스터 내부에서만 접근 가능
  type: ClusterIP
