apiVersion: apps/v1
kind: Deployment
metadata:
  name: radius-signaling
  namespace: radius
spec:
  replicas: 2
  selector:
    matchLabels:
      app: radius-signaling
  template:
    metadata:
      labels:
        app: radius-signaling
    spec:
      containers:
        - name: signaling
          image: ghcr.io/yoon6yo/radius-be:latest
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: NODE_ENV
              value: "production"
            - name: REDIS_URL
              value: "redis://redis:6379"
            - name: RATE_LIMIT_WINDOW_MS
              value: "600000"
            - name: RATE_LIMIT_MAX_REQUESTS
              value: "20"
            - name: CORS_ORIGIN
              valueFrom:
                secretKeyRef:
                  name: radius-signaling-secret
                  key: cors-origin
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
